/target
.env
backups/
.DS_Store

# Local tool databases / stray artifacts
webfind.db
pagerank_cache.json

# Frontend build output (belt-and-suspenders; frontend/.gitignore also covers)
frontend/node_modules
frontend/dist
